const fadeInOut = {
  in: {
    opacity: 1,
  },
  out: {
    opacity: 0,
  }
}

const scaleInOut = {
  in: {
    scale: 1,
  },
  out: {
    scale: 0,
  }
}

const scaleFadeInOut = {
  in: {
    scale: 1,
    opacity: 1,
  },
  out: {
    scale: 0,
    opacity: 0,
  }
}

export default {
  fadeInOut,
  scaleInOut,
  scaleFadeInOut,
}
